# Required — get one at https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-REPLACE_ME

# Optional — override the model. Default: claude-sonnet-4-6
# GURU_MODEL=claude-sonnet-4-6

# Optional — rate limit (requests per window per IP). Defaults: 20 / 60s.
# RATE_LIMIT_MAX=20
# RATE_LIMIT_WINDOW_MS=60000
